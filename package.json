{
  "name": "me",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "npm run build:tokens && astro dev",
    "build": "npm run build:tokens && astro build",
    "build:tokens": "node src/build-tokens.ts",
    "preview": "npm run build:tokens && netlify serve",
    "test:a11y": "npm run build && (axe ./dist || true)",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/netlify": "^7.0.10",
    "@astrojs/sitemap": "^3.7.2",
    "@lucide/astro": "^1.16.0",
    "astro": "^6.3.4",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@axe-core/cli": "^4.11.3",
    "sharp": "^0.34.5",
    "style-dictionary": "^5.4.1"
  }
}
